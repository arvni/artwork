# Material UI - Create React App example in TypeScript

## How to use

Download 

<!-- #default-branch-switch -->

```bash
tar -xz --strip=2 artwork.gzip
cd artwork
```

Install it and run:

```bash
npm install
npm start
```